class CSP(search.problem):
	def __init__(self, items, bags):
		self.bags = bags
        self.items = items
        
        for item.name is self.items:
            self.items[item_name].possible_bags = self.bags.copy()

